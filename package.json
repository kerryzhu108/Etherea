{
  "scripts": {
    "dev": "cd backend && npm install && npm run dev",
    "start": "cd backend && npm install && npm start"
  },
  "dependencies": {
    "axios": "^0.24.0",
    "bcrypt": "^5.0.1",
    "dotenv": "^10.0.0",
    "ejs": "^3.1.6",
    "multer": "^1.4.3",
    "node-fetch": "^3.0.0",
    "pg": "^8.7.1",
    "postgres": "^1.0.2",
    "request-json": "^0.6.5"
  }
}
