{
  "scripts": {
    "start": "cd backend && npm install && npm start"
  },
  "dependencies": {
    "bcrypt": "^5.0.1",
    "dotenv": "^10.0.0",
    "ejs": "^3.1.6",
    "pg": "^8.7.1",
    "postgres": "^1.0.2"
  }
}
